import React from 'react'

function Task(tasks) {
    return (/*
        <div className="container">
        {               
            tasks.map((task)=>{   
        return(             
        <div className="list-group list-group-flush" key={task.id}>   
            <div className="list-group-item d-flex justify-content-between align-content-center">
            <p>{task.task}</p>
            </div>  
        </div>
            )})
        }
    </div>*/
    <h1>hello world</h1>
    )
}

export default Task
